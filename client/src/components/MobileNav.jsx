import React from 'react';

// MobileNav is hidden/disabled because all navigation buttons are now available in the top menu drawer for screens <= 1252px
export default function MobileNav() {
  return null;
}
